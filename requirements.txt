# Python dependencies for claudePlugins

# Required for notification-hook plugin
playsound3>=3.0.0

# Testing dependencies (optional, for development)
pytest>=7.0.0
pytest-cov>=4.0.0

# Linting and formatting (optional, for development)
pylint>=2.0.0
black>=22.0.0
